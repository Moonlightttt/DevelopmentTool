apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "development-tool.fullname" . }}-test-connection"
  labels:
    {{- include "development-tool.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "development-tool.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
